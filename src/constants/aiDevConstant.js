const AI_DEV_TYPES = {
  FEATURE: "feature",
  REVIEW: "review",
  REFACTOR: "refactor",
  BUGFIX: "bugfix",
};

const VALID_AI_DEV_TYPES = Object.values(AI_DEV_TYPES);

module.exports = {
  AI_DEV_TYPES,
  VALID_AI_DEV_TYPES,
};
